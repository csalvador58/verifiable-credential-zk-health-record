[package]
name = "validate"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "0.18.0", default-features = false, features = [ "std" ] }
json = "0.12"
json-core = { path = "../../core" }
